import React from 'react';
import { View } from 'react-native';

const Separator = () => {
    return <View style={{ height: 12, backgroundColor: '#111', width: '100%', marginTop: 12 }} />;
};

export default Separator;
